---
"@vgpu/wgsl-std": patch
---

The noise docs now state that there is no Perlin, simplex, fBM, or value-noise primitive and show the octave-plus-domain-warp recipe that produces a cloud/plasma look from `voronoi3d`, plus a note that the package ships as a dependency of `vgpu`.
