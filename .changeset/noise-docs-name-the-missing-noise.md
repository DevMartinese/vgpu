---
"@vgpu/wgsl-std": patch
---

The `@vgpu/wgsl-std/noise` (Voronoi) docs now show the octave-plus-domain-warp recipe that produces a cloud/plasma look from `voronoi3d` and cross-link to the dedicated `noise/perlin`/`noise/simplex` subpaths for smooth gradient noise, plus a note that the package ships as a dependency of `vgpu`.
