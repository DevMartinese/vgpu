---
"@vgpu/render": patch
---

Respell the published README and reference docs for the 0.2.0 free-function API (`surface(gpu, canvas)`, `draw(gpu, …)`, `frameLoop(gpu, cb)`, `geometry(gpu, …)`), so copied snippets compile. No runtime change: `@vgpu/render`'s own exports are untouched.
